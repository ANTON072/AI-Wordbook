<!-- docs/steering/[YYYYMMDD]-[開発タイトル]/tasklist.md の骨格。
     作業中に進捗を更新していくファイル。plan.md の受け入れ条件・設計を満たすタスクに分解する。この注記コメント自体は生成物に残さない。

     [HUMAN] タスクについて：
     - AWS設定・デプロイ実行・シークレット入力など、人間が手動で行う必要があるタスクには先頭に [HUMAN] を付ける。
     - Claude は [HUMAN] タスクに到達したら作業を停止し、ユーザーに操作内容を伝えて完了報告を待つ。
     - ユーザーが完了を報告したら [HUMAN] タスクをチェック済みにして次のタスクへ進む。
     - セッションを再開する場合は、tasklist.md を読んで未完了の最初のタスクから再開する。 -->

# タスクリスト

## 実装タスク

<!-- [HUMAN] タスクの例：
- [ ] **[HUMAN]** AWS Console で Cognito User Pool を作成し、クライアントIDを `.env` に設定する → 完了したら報告してください
Claude はここで停止し、ユーザーの完了報告を受けてから次へ進む。 -->

- [ ]

## テスト

<!-- 実装タスクが完了したら、write-tests スキルを読んで今回の実装に対応するテストを記述する。
     実行は品質チェック（pnpm check）で行うため、ここでは記述のみでよい。
     テスト項目は plan.md の受け入れ条件から導出して具体的に列挙する。 -->

- [ ] 

## コードレビューループ

<!-- 実装タスクが全て完了したら、以下の手順を実行する。
     `code-reviewer` はサブエージェント（バックグラウンド）として実行されるため、完了通知を受け取るまで
     品質チェックへ進んではならない。1周目の指摘はユーザーに提示してから修正を始める。
     重大度高（セキュリティ・設計違反）は自動修正し、判断が分かれる指摘はユーザーに確認してから対応する。
     2周目以降は残存指摘がなくなるまで続ける。 -->

- [ ] **1周目**: `code-reviewer` エージェントを起動 → 完了通知を受け取ってから指摘一覧をユーザーに提示
  - 重大度高（セキュリティ・層境界違反）: 自動修正
  - 判断が分かれる指摘: ユーザーに確認してから対応
- [ ] **2周目以降**: 残存指摘がなくなるまで繰り返す（各周、修正内容をユーザーに提示）
- [ ] 対応困難な指摘はその理由をユーザーに報告
- [ ] 全指摘への対応完了（残存指摘ゼロ、または対応困難分をユーザーに報告済み）✅

## 品質チェック

<!-- **コードレビューループの全指摘対応が完了してから**このセクションに入る。
     `pnpm check` が all green になるまで繰り返す。エラーが残る場合は自動修正して再実行する。 -->

- [ ] `pnpm check`（lint・format・test・typecheck の一括実行）を実行
  - エラーがあれば自動修正して再実行し、all green になるまで繰り返す
- [ ] `pnpm check` all green ✅

## 進捗状況
