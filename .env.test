# vitest 実行時（NODE_ENV=test）に読み込まれる環境変数
# シークレットを含まない値のみ記載する（.env.local はテスト時にロードされない）
# Cognito 関連の実値は不要（テストでは JWT を直接組み立てるため）

DYNAMODB_TABLE_NAME=WordbookTest
AWS_REGION=ap-northeast-1

DYNAMODB_LOCAL_ENDPOINT=http://localhost:8000
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
